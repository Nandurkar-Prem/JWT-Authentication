spring.application.name=jwt-authentication

#Very important thing before starting operation first we need to configure the DB and then things get going
#This the DB URL to access the DB from MySQL server
spring.datasource.url=jdbc:mysql://localhost:3306/jwt_authentication 
#This is teh username of the DB connection that is default in MySQL
spring.datasource.username=root
#This is the personal password for the connection that is used here
spring.datasource.password=King@18prem

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

jwt.secret=premnandurkarisalegendandheisoutoftheworld
jwt.expiration=3600000
jwt.refresh-expiration=604800000